# Binary, built via `go build`
terraform-provider-claude-managed-agents

# Built provider binaries for local install
dist/

# Local dev tooling installed by `make tools`
bin/

# Test artifacts
*.test
*.out
coverage.txt
coverage.html
coverage.out
coverage-acc.out

# Terraform state from acceptance scratch dirs
*.tfstate
*.tfstate.*
.terraform/
.terraform.lock.hcl
crash.log
crash.*.log

# Editor & OS
.idea/
.vscode/
.DS_Store
.envrc

# Local development overrides
.terraformrc
terraform.tfrc

# Secrets (never commit)
.env
*.pem

# Internal AI scratch — local-only subpaths under .claude/
# (.claude/routines/ IS committed for reproducibility — see its README.md)
.claude/plans/
.claude/worktrees/
.claude/settings.local.json

# Local Claude Code / playwright-mcp scratch artifacts — never commit.
.claude/scheduled_tasks.lock
.playwright-mcp/
