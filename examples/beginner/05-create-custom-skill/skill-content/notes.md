# Notes

A sibling file Claude can read on demand by referencing its path from
`SKILL.md`. Try editing this file and running `terraform apply` — the
provider will detect the content change and push a new skill version.
