---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "Claude Managed Agents Provider"
description: |-
  Terraform provider for Anthropic Claude Managed Agents (community, unofficial).
---

# Claude Managed Agents Provider

Terraform provider for Anthropic Claude Managed Agents (community, unofficial).

## Example Usage

```terraform
terraform {
  required_version = ">= 1.11"

  required_providers {
    claude-managed-agents = {
      source  = "andasv/anthropic-claude-managed-agents"
      version = "~> 0.2"
    }
  }
}

# The provider reads ANTHROPIC_API_KEY from the environment by default.
# Prefer the env var over committing keys to HCL — required.
provider "claude-managed-agents" {
  # api_key     = var.anthropic_api_key
  # base_url    = "https://api.anthropic.com"
  # max_retries = 3
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `api_key` (String, Sensitive) Anthropic API key. Defaults to the `ANTHROPIC_API_KEY` environment variable. Marked sensitive: not shown in plan output.
- `base_url` (String) API base URL. Defaults to `https://api.anthropic.com`. Override for self-hosted gateways or to point at a local test server.
- `max_retries` (Number) Maximum number of retries for transient failures (5xx, 429). Defaults to 3.
