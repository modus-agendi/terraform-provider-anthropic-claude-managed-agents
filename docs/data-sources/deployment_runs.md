---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "claude-managed-agents_deployment_runs Data Source - Claude Managed Agents"
subcategory: ""
description: |-
  List append-only run records for deployments. Each scheduled or manual fire produces one run; exactly one of session_id (success) or error_type (failure) is set.
---

# claude-managed-agents_deployment_runs (Data Source)

List append-only run records for deployments. Each scheduled or manual fire produces one run; exactly one of `session_id` (success) or `error_type` (failure) is set.

## Example Usage

```terraform
terraform {
  required_providers {
    claude-managed-agents = {
      source  = "modus-agendi/anthropic-claude-managed-agents"
      version = "~> 0.4"
    }
  }
}

provider "claude-managed-agents" {}

# List the append-only run records for a deployment. Each scheduled or manual
# fire produces one run; exactly one of `session_id` (success) or `error_type`
# (failure) is set on each.
data "claude-managed-agents_deployment_runs" "failures" {
  deployment_id = "deployment_01HqR2k7vXbZ9mNpL3wYcT8f"

  # Optional filters (all may be combined):
  trigger_type = "schedule" # "schedule" or "manual"
  has_error    = true       # true = failed runs only, false = successful only
  limit        = 50         # default 20, max 1000 (first page only)
}

# Surface the typed failure reasons so you can alert on them.
output "recent_failures" {
  value = [
    for run in data.claude-managed-agents_deployment_runs.failures.runs : {
      id         = run.id
      error_type = run.error_type
      at         = run.created_at
    }
  ]
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `deployment_id` (String) Filter to a single deployment. Omit to list runs across all deployments in the workspace.
- `has_error` (Boolean) Filter: `true` for failed runs, `false` for successful runs. Omit for all.
- `limit` (Number) Maximum number of runs to return (default 20, max 1000). Only the first page is fetched.
- `trigger_type` (String) Filter by trigger: `schedule` or `manual`. Omit for all.

### Read-Only

- `runs` (Attributes List) The matching run records, newest first. (see [below for nested schema](#nestedatt--runs))

<a id="nestedatt--runs"></a>
### Nested Schema for `runs`

Read-Only:

- `agent_id` (String) Resolved agent id.
- `agent_version` (Number) Resolved agent version.
- `created_at` (String) ISO 8601 run timestamp.
- `deployment_id` (String) Owning deployment id.
- `error_message` (String) Human-readable failure detail, else null.
- `error_type` (String) Typed failure reason on failure, else null.
- `id` (String) Run id (`drun_...`).
- `scheduled_at` (String) Scheduled fire time (schedule trigger only), else null.
- `session_id` (String) Created session id on success, else null.
- `trigger_type` (String) `schedule` or `manual`.
